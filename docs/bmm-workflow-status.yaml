# Workflow Status - Steri-Connect-Melag-Getinge-GO
# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-11-21T09:20:54.388Z"
project: "Steri-Connect-Melag-Getinge-GO"
project_type: "greenfield"
selected_track: "bmad-method"
field_type: "greenfield"
workflow_path: ".bmad/bmm/workflows/workflow-status/paths/method-greenfield.yaml"

workflow_status:
  - phase: 0
    name: "Discovery (Optional)"
    optional: true
    workflows:
      - id: "brainstorm-project"
        status: "optional"
        agent: "analyst"
        command: "brainstorm-project"
        included_by: "user_choice"
        note: "Selected during workflow-init"

      - id: "research"
        status: "docs/research-technical-device-interfaces-2025-11-21.md"
        agent: "analyst"
        command: "research"
        included_by: "user_choice"
        note: "Selected during workflow-init - Useful for Melag/Getinge interface research"

      - id: "product-brief"
        status: "docs/product-brief-Steri-Connect-Melag-Getinge-GO-2025-11-21.md"
        agent: "analyst"
        command: "product-brief"
        included_by: "user_choice"
        note: "Selected during workflow-init - Recommended for greenfield Method projects"

  - phase: 1
    name: "Planning"
    required: true
    workflows:
      - id: "prd"
        status: "required"
        agent: "pm"
        command: "prd"
        output: "Product Requirements Document with FRs and NFRs"
        note: "PRD already exists - may need validation/refinement"

      - id: "validate-prd"
        status: "optional"
        agent: "pm"
        command: "validate-prd"
        note: "Quality check for PRD completeness"

      - id: "create-design"
        status: "conditional"
        condition: "if_has_ui"
        agent: "ux-designer"
        command: "create-design"
        note: "Determined after PRD - user/agent decides if needed (Steri-Suite UI required)"

  - phase: 2
    name: "Solutioning"
    required: true
    workflows:
      - id: "create-architecture"
        status: "docs/architecture.md"
        agent: "architect"
        command: "create-architecture"
        output: "System architecture document"
        note: "Complete system design for greenfield projects - Critical for 3-tier architecture"

      - id: "create-epics-and-stories"
        status: "docs/epics.md"
        agent: "pm"
        command: "create-epics-and-stories"
        note: "Required: Break down PRD into implementable epics and stories with full context (PRD + UX + Architecture)"

      - id: "test-design"
        status: "docs/test-design-system.md"
        agent: "tea"
        command: "test-design"
        output: "System-level testability review"
        note: "Testability assessment before gate check - auto-detects system-level mode"

      - id: "validate-architecture"
        status: "optional"
        agent: "architect"
        command: "validate-architecture"
        note: "Quality check for architecture completeness"

      - id: "implementation-readiness"
        status: "docs/implementation-readiness-report-2025-11-21.md"
        agent: "architect"
        command: "implementation-readiness"
        note: "Validates PRD + UX + Architecture + Epics + Testability cohesion before implementation"

  - phase: 3
    name: "Implementation"
    required: true
    workflows:
      - id: "sprint-planning"
        status: "docs/sprint-artifacts/sprint-status.yaml"
        agent: "sm"
        command: "sprint-planning"
        note: "Creates sprint plan - subsequent work tracked there"

